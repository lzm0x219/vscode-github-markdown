name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_call:

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - uses: jdx/mise-action@v4

      - name: Install nub
        run: npm install --global @nubjs/nub

      - run: pnpm install --frozen-lockfile

      - name: Cache Chromium
        uses: actions/cache@v6
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml') }}

      - name: Install Chromium
        run: pnpm exec playwright install --with-deps chromium

      - name: Lint
        run: nubx oxlint .

      - name: Format check
        run: nubx oxfmt --check .

      - name: Lint (type-aware)
        run: nubx oxlint --type-aware .

      - name: Test
        run: nub run test

      - name: Verify Markdown parity baseline
        run: pnpm run verify:parity

      - name: Upload Markdown parity images
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: markdown-parity-${{ github.run_id }}
          path: artifacts/parity
          if-no-files-found: ignore

      - name: Build
        run: nub run build

  host:
    name: Host smoke (${{ matrix.name }})
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: desktop-minimum
            kind: desktop
            vscode: 1.74.0
          - name: desktop-stable
            kind: desktop
            vscode: 1.129.0
          - name: web-stable
            kind: web

    steps:
      - uses: actions/checkout@v7

      - uses: jdx/mise-action@v4

      - name: Install nub
        run: npm install --global @nubjs/nub

      - run: pnpm install --frozen-lockfile

      - name: Cache Chromium
        if: matrix.kind == 'web'
        uses: actions/cache@v6
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml') }}

      - name: Install Chromium
        if: matrix.kind == 'web'
        run: pnpm exec playwright install --with-deps chromium

      - name: Desktop Extension Host smoke test
        if: matrix.kind == 'desktop'
        env:
          VSCODE_TEST_VERSION: ${{ matrix.vscode }}
        run: xvfb-run -a nub run test:host:desktop

      - name: Desktop final Markdown preview regression
        if: matrix.name == 'desktop-stable'
        env:
          VSCODE_TEST_VERSION: ${{ matrix.vscode }}
        run: xvfb-run -a nub run test:host:desktop:preview

      - name: Web Extension Host smoke test
        if: matrix.kind == 'web'
        run: nub run test:host:web

      - name: Web final Markdown preview regression
        if: matrix.kind == 'web'
        run: nub run test:host:web:preview
